  name: "Manual Hello World"

    on:
      workflow_dispatch:

    jobs:
      say-hello:
        runs-on: ubuntu-latest
        steps:
          - name: "Say Hello"
            run: echo "Hello, Actions! The time is $(date)."
